1.1.1) Para iniciar un repositorio local se debe ejecutar el comando "Git Init".

1.1.2) Para que un directorio deje de ser controlado por git debemos usar el comando "git rm --r <Directorio>".

1.1.3) Si, una vez que un directorio está controlado por git, todo archivo creado dentro de este tambien estara controlado por el repositorio Git.

1.1.4) Para agregar un archivo a un repositorio local de git hay que colocar el comando "Git add <Archivo>".

1.1.5) El comando que se debe usar para determinar el estado de los archivos es "Git status".

1.1.6) Para realizar el commit de un archivo hay que colocar "Git commit" y luego el nombre del archivo del que se desea realizar el commit.

1.1.7) Un commit en Git es como una captura que realiza a un archivo determinado en un momento determinado para guardarlo y subirlo al repositorio, algo asi como una copia de seguridad que debe estar acompañada de un mensaje para guardarse.

1.2.3) La salida significa que el archivo sandwich.txt esta modificado.

1.2.5) Una vez que usamos el comando Add estamos pasando del estado Modified al Stage, preparando el archivo y estando al paso previo de usar el comando commit.

1.2.7) Luego nuestro archivo recibe el commit limpiandose de nuestro Working Three, aclarando los archivos que se modificaron, la cantidad de inserciones y palabras borradas.

1.2.9) Al ejecutar git log me aparecen todos los archivos commiteados

1.2.10) Al ejecutar git log "Nombre Del Archivo", me da el codigo con el que esta identificado el commit de dicho archivo.
	1: Al ejecutar git log --oneline solo muestra el nombre de los archivos commiteados prdenados del mas reciente al mas antiguo.

	2: Al ejecutar git log --stat se muestran los archivos commit con sus estadisticas asi como dice el nombre, expresando asimismo los cambios que se realizaron en el commit.

1.2.14) Una vez commiteados todos los archivos figura un mensaje de parte de Git que dice que el arbol de trabajo está limpio y que por ende, no hay archivos modificados, añadidos o commiteados.

1.2.15) Luego de usar el comando rm, el archivo aparece como removido pero se mantiene en la lista de cambios para commitear

1.2.16) Aunque el archivo esté borrado, en el commit de la bitacora usando el ultimo comando sigue figurando, por ende aunque el archivo se alla subido estando borrado, se pudo realizar un commit y permanece ahi
